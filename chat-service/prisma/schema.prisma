datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Message {
  id         String   @id @default(uuid())
  senderId   String
  senderName String
  content    String
  createdAt  DateTime @default(now())

  @@index([createdAt])
  @@map("messages")
}
